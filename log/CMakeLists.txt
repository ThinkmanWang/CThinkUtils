project(log)
AUX_SOURCE_DIRECTORY(. log_SOURCE_DIR)
ADD_LIBRARY (log log.c)
TARGET_LINK_LIBRARIES(log ${GLIB2_LIBRARIES} ${GTHREAD2_LIBRARIES})
INCLUDE_DIRECTORIES(${GLIB2_INCLUDE_DIR}
        ${JsonGlib_INCLUDE_DIRS})